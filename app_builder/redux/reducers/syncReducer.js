export default function syncReducer(sync=[],action){
    let new_sync_state = [];
    var array =sync;
    var temp;
    switch(action.type){

        case'LOGIN':

     default:
           // console.log("in login")
            return sync
    }
}